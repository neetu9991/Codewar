# Narcissistic number is a positive number which is the sum of it's own digits,
# each raised to power of number of digits in a given base
# 1^3 + 5^3 + 3^3 = 1 + 125 + 27 = 153


def narcissistic(value):
return sum(int(i)**len(str(value)) for i in str(value)) == value 
